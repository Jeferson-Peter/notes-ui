// pages/tags.tsx
import { NextPage } from 'next';
import TagsComponent from '@/components/Tag'; // Adjust the import path according to your project structure

const TagsPage: NextPage = () => {
  return (
    <TagsComponent />
  );
};

export default TagsPage;
